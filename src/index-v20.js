import live from './index-v13.js';
import { backfill2h } from './backfill-2h.js';
import { fetchPusholder } from './pusholder.js';
import { enrichNews } from './openrouter.js';
const esc=s=>String(s||'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;'),clean=s=>String(s||'').replace(/\s+/g,' ').trim(),norm=s=>clean(s).toLocaleLowerCase('tr-TR').replace(/[^a-z0-9çğıöşü ]/gi,' ').replace(/\s+/g,' ').trim(),words=s=>new Set(norm(s).split(' ').filter(x=>x.length>3));
function similar(a,b){const A=words(a),B=words(b);let n=0;for(const x of A)if(B.has(x))n++;return A.size&&B.size&&n>=3&&n/Math.min(A.size,B.size)>=.38}async function hash(s){const d=await crypto.subtle.digest('SHA-256',new TextEncoder().encode(norm(s)));return[...new Uint8Array(d)].map(x=>x.toString(16).padStart(2,'0')).join('').slice(0,48)}
function category(i){const n=norm(i.title+' '+i.description);if(/enflasyon|faiz|dolar|euro|altın fiyat|gram altın|borsa|ekonomi/.test(n))return'EKONOMİ';if(/futbol|voleybol|basketbol|şampiyon|final|transfer|hakem|süper lig/.test(n))return'SPOR';if(/savaş|ateşkes|iran|israil|gazze|rusya|ukrayna|abd|suriye/.test(n))return'DÜNYA';if(/cumhurbaşkan|bakan|tbmm|meclis|parti|seçim/.test(n))return'SİYASET';return'GÜNDEM'}
function tags(i){return(i.ai_hashtags?.length?i.ai_hashtags:['#Gündem']).filter(x=>/^#[\p{L}\p{N}_]{2,30}$/u.test(String(x))).slice(0,3)}function short(s,n=62){s=clean(s);if(s.length<=n)return s;const c=s.slice(0,n+1),p=c.lastIndexOf(' ');return c.slice(0,p>28?p:n).replace(/[,:;\-]+$/,'')}function xtext(i){const hs=tags(i).join(' ');return`🔴 ${short(i.title,78)}\n\n${hs}`.slice(0,190)}
function visualBody(i){let s=clean(i.description);if(s.length<=430)return s;const c=s.slice(0,431),p=Math.max(c.lastIndexOf('. '),c.lastIndexOf('! '),c.lastIndexOf('? '));if(p>180)return c.slice(0,p+1);const w=c.lastIndexOf(' ');return c.slice(0,w>180?w:430).replace(/[,:;\-]+$/,'')+'.'}
function html(i){const photo=i.image?`<img class="p" src="${esc(i.image)}">`:'';return`<!doctype html><html><head><meta charset="utf-8"><style>*{box-sizing:border-box}body{margin:0;width:1080px;height:1080px;background:#080a0f;color:#fff;font-family:Arial,sans-serif}.c{position:relative;width:1080px;height:1080px;overflow:hidden}.p{position:absolute;width:100%;height:610px;object-fit:cover}.shade{position:absolute;inset:0;background:linear-gradient(180deg,rgba(0,0,0,.08),rgba(0,0,0,.18) 38%,#080a0f 61%)}.logo{position:absolute;top:38px;left:42px;background:#080a0fe8;padding:15px 22px;border-radius:30px;font-size:29px;font-weight:1000}.logo em{color:#ef233c;font-style:normal}.breaking{position:absolute;top:42px;right:42px;background:#ed1b2e;padding:13px 22px;font-size:22px;font-weight:1000}.q{position:absolute;left:48px;right:48px;bottom:38px}.cat{display:inline-block;background:#ed1b2e;padding:9px 16px;font-size:17px;font-weight:1000}.t{font-size:47px;line-height:1.04;font-weight:1000;text-transform:uppercase;margin:15px 0 13px;max-height:148px;overflow:hidden;text-shadow:0 3px 12px #000}.line{width:145px;height:6px;background:#ed1b2e;margin-bottom:15px}.d{font-size:23px;line-height:1.31;max-height:210px;overflow:hidden;color:#f2f2f2}.f{margin-top:18px;padding-top:13px;border-top:1px solid #3b3b3b;display:flex;justify-content:space-between;font-size:17px;font-weight:900}.handle{color:#ffd43b}</style></head><body><div class="c">${photo}<div class="shade"></div><div class="logo">90<em>+</em> GÜNDEM</div><div class="breaking">SON DAKİKA</div><div class="q"><div class="cat">${category(i)}</div><div class="t">${esc(short(i.title,105))}</div><div class="line"></div><div class="d">${esc(visualBody(i))}</div><div class="f"><span>90+ GÜNDEM</span><span class="handle">@90plusgndm</span></div></div></div></body></html>`}
async function state(env,key){return(await env.DB.prepare('SELECT value FROM worker_state WHERE key=?').bind(key).first())?.value||null}async function setState(env,key,value){await env.DB.prepare('INSERT INTO worker_state(key,value,updated_at) VALUES(?,?,CURRENT_TIMESTAMP) ON CONFLICT(key) DO UPDATE SET value=excluded.value,updated_at=CURRENT_TIMESTAMP').bind(key,String(value)).run()}
async function send(env,i){const chat=env.TELEGRAM_CHAT_ID||await state(env,'telegram_chat_id');if(!chat||!env.TELEGRAM_BOT_TOKEN||!env.BROWSER)throw new Error('telegram_or_browser_not_configured');const r=await env.BROWSER.quickAction('screenshot',{html:html(i),viewport:{width:1080,height:1080},screenshotOptions:{type:'png',fullPage:false}});if(!r.ok)throw new Error('browser_'+r.status);const x=xtext(i),f=new FormData();f.append('chat_id',String(chat));f.append('photo',new Blob([new Uint8Array(await r.arrayBuffer())],{type:'image/png'}),`90gundem-pusholder-${i.pusholder_id}.png`);f.append('caption',x);f.append('reply_markup',JSON.stringify({inline_keyboard:[[{text:'𝕏 X’TE PAYLAŞ',url:`https://twitter.com/intent/tweet?text=${encodeURIComponent(x)}`}]]}));const t=await fetch(`https://api.telegram.org/bot${env.TELEGRAM_BOT_TOKEN}/sendPhoto`,{method:'POST',body:f}),j=await t.json();if(!j.ok)throw new Error('telegram:'+j.description);return true}
async function pusholderRun(env,manual=false){await env.DB.prepare('CREATE TABLE IF NOT EXISTS worker_state(key TEXT PRIMARY KEY,value TEXT NOT NULL,updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP)').run();await env.DB.prepare('CREATE TABLE IF NOT EXISTS speed_queue(fingerprint TEXT PRIMARY KEY,title TEXT NOT NULL,status TEXT NOT NULL,created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP)').run();const started=new Date().toISOString();await setState(env,'pusholder_heartbeat',started);let items=[];try{items=await fetchPusholder()}catch(e){const out={status:'error',version:'pusholder-fastlane-v5',manual,at:started,error:String(e?.message||e),telegram_notified:0};await setState(env,'pusholder_status',JSON.stringify(out));throw e}const cursor=Number(await state(env,'pusholder_last_id')||0),latest=items.reduce((m,x)=>Math.max(m,Number(x.pusholder_id)||0),0);if(!cursor){if(latest)await setState(env,'pusholder_last_id',latest);const out={status:'ok',version:'pusholder-fastlane-v5',manual,at:started,mode:'cursor_initialized',scanned:items.length,last_id:latest,new_items:0,telegram_notified:0,dedupe_skipped:0,failed:0};await setState(env,'pusholder_status',JSON.stringify(out));return out}const pending=items.filter(i=>Number(i.pusholder_id)>cursor).sort((a,b)=>Number(a.pusholder_id)-Number(b.pusholder_id)).slice(0,12);let notified=0,skipped=0,failed=0,lastProcessed=cursor,errors=[];for(const p of pending){const id=Number(p.pusholder_id);try{const key=`pusholder:${p.pusholder_id}`,seen=await state(env,key);if(seen){skipped++;lastProcessed=Math.max(lastProcessed,id);continue}const duplicateRows=await env.DB.prepare("SELECT title FROM speed_queue WHERE created_at >= datetime('now','-90 minutes') ORDER BY created_at DESC LIMIT 80").all();if((duplicateRows.results||[]).some(x=>similar(x.title,p.title))){await setState(env,key,'duplicate');skipped++;lastProcessed=Math.max(lastProcessed,id);continue}let i=p;if(env.OPENROUTER_API_KEY)i={...p,...await enrichNews(env,{primary:p,items:[p]}),image:p.image,link:p.link,pusholder_id:p.pusholder_id};const fp=await hash(i.title);i={...i,fp};await send(env,i);notified++;await env.DB.prepare("INSERT OR IGNORE INTO speed_queue(fingerprint,title,status) VALUES(?,?,'share_ready')").bind(fp,i.title).run();await setState(env,key,'sent');lastProcessed=Math.max(lastProcessed,id)}catch(e){failed++;errors.push(`${p.pusholder_id}:${String(e?.message||e).slice(0,100)}`);break}}if(lastProcessed>cursor)await setState(env,'pusholder_last_id',lastProcessed);const out={status:failed?'partial':'ok',version:'pusholder-fastlane-v5',manual,at:started,scanned:items.length,last_seen_id:latest,cursor_before:cursor,cursor_after:lastProcessed,new_items:pending.length,telegram_notified:notified,dedupe_skipped:skipped,failed,errors:errors.slice(0,3)};await setState(env,'pusholder_status',JSON.stringify(out));return out}
async function monitor(env){try{return await pusholderRun(env,false)}catch(e){return{status:'error',error:String(e?.message||e)}}}
export default{async fetch(req,env,ctx){const u=new URL(req.url);if(u.pathname==='/backfill-2h'){try{return Response.json(await backfill2h(env))}catch(e){return Response.json({status:'error',version:'backfill-2h-v1',error:String(e?.message||e)},{status:500})}}if(u.pathname==='/pusholder-test'){try{return Response.json(await pusholderRun(env,true))}catch(e){return Response.json({status:'error',version:'pusholder-fastlane-v5',error:String(e?.message||e)},{status:500})}}if(u.pathname==='/pusholder-status'){const raw=await state(env,'pusholder_status'),heartbeat=await state(env,'pusholder_heartbeat'),cursor=await state(env,'pusholder_last_id');return Response.json({status:raw?'ok':'waiting',version:'pusholder-fastlane-v5',heartbeat,last_id:cursor,last_run:raw?JSON.parse(raw):null})}if(u.pathname==='/health'){const r=await live.fetch(req,env,ctx);let d={};try{d=await r.clone().json()}catch{}return Response.json({...d,version:'speed-publish-v25',pusholder_fastlane:true,pusholder_mode:'persistent-id-cursor',pusholder_poll_minutes:2,pusholder_catchup:true,pusholder_max_batch:12,visual_full_news:true,visual_hashtags:false,x_mode:'short-headline-plus-hashtags'})}return live.fetch(req,env,ctx)},async scheduled(event,env,ctx){ctx.waitUntil(monitor(env));ctx.waitUntil(live.scheduled(event,env,ctx))}};