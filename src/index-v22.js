// +90Gündem V22 entrypoint.
// V22 is intentionally isolated from the V21 branch entrypoint so preview/deploy
// configuration can target the AI newsroom without renaming the proven V21 file.
export { default } from './index-v21.js';
